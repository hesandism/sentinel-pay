# SentinelPay — dashboard-only pins (Phase 8)
# ============================================
# The Streamlit dashboard reads Postgres/Prometheus/MLflow over the network —
# it needs NONE of the ML stack (lightgbm, shap, mlflow, evidently). Keeping
# its image on this slim set builds in seconds and stays small.
# Versions are kept in lockstep with requirements.txt.
streamlit==1.35.0
altair==5.5.0                 # charts (a streamlit dependency, pinned explicitly)
pandas==2.2.2
numpy==1.26.4
psycopg2-binary==2.9.9
requests==2.32.2
